# Stage 8F-A — 原信号方向／路径标签与成本边界研究

状态：本阶段研发研究已运行；等待验收，不进入 8F-B。
作用域：`READ_ONLY_RESEARCH / NOT_ADMISSION`。
仓库：`RoeKai/SOL_TradingAI`；独立分支：`phase-08fa-signal-cost-research`。
唯一基线：`a6c68008327f8aac2fd888572e33504ff20cd019`。

## 1. 结论先分开：方向线索不等于可以交易

研究线 A：冻结的 15 分钟主指标——同方向、同 UTC 日／小时／事前波动层内，
按候选数加权的信号减背景平均变化——为 **+1.1535 bps**，配对日块区间
**[+0.4323, +1.8018] bps**。达到事先固定的“研发方向线索”描述标准，
不是策略有效性或真实交易资格。主要来自 SHORT；LONG 主窗口区间跨零。
固定每方向每 15 分钟仅取最早候选后，点差只有 **+0.2082 bps**，
说明结论对密集、重叠候选的权重有明显依赖；该敏感性没有另算置信区间。

研究线 B：在极乐观的“只计原声明手续费”反事实下，**27,379 / 37,141
（73.7164%）** 的原目标净收益仍不为正；另 **9,468** 个虽为正却达不到原
静态净 RR 门槛。仅 **294** 个满足这个静态必要条件。保留原资金费预算和
价差、仅把每腿滑点降到 0 bps 后，仅 **6** 个满足静态必要条件；这不是
6 个可交易机会。当前 10 bps 条件下的 **2** 个静态必要条件正例仍分别有
原 BTC 风控拒绝与原 S3／求解限制，没有新审批。

两条线不能合并成“信号能赚钱”：价格标签不是成交，成本档位没有经账户／
盘口／自有成交标定；约 90.77% 的有效主窗口候选先观察到原结构失效。
MFE 或较晚反弹不能补算为失效前的机会。没有修改任何策略、止损、目标、
退出比例、评分、RR 门槛、资金费预算或正式执行成本。

## 2. 协议、代码、数据与结果版本

先提交协议，再开发合成测试，之后才计算本轮新八月标签／敏感性结果。
协议是对**已经研究过的八月研发集**固定本轮新增方法，不是对未知八月的
事前策略注册。协议正文未因结果变化而修改。

| 对象 | 版本／摘要 |
| --- | --- |
| 协议文件 | `docs/STAGE_08FA_RESEARCH_PROTOCOL.md` |
| 协议独立提交 | `d00bf544eec2c5cd918af2cf478c7c0081a4803d` |
| 协议 SHA-256 | `1374bcdb5580f86c5752bcf25c23c159f8f610a18358a2ca165ff93cd203d743` |
| 保留失败原型提交 | `fbf2e6de3ae96050d3beac9a5aeece7816cab1d5` |
| 实际八月计算代码提交 | `783a3212b26e142a11cd308713738e313f43737c` |
| 该次研究代码内容摘要 | `5449337badd4e74b1ba7c5800298fc7003fc8ab80c01d9e3c2b893a2a83b504f` |
| 新结果目录／模型 | `research-runs/august-8fa-v1` / `stage08fa-readonly-research/v1` |
| 原数据范围 | `[2026-08-01T00:00:00Z, 2026-09-01T00:00:00Z)`，原 300 秒预热 |
| 数据用途 | `DEVELOPMENT / ALREADY_EXAMINED` |
| 数据摘要 | `2bd4ab5b4cdca36938dbb32aa01214d8cf5cfd236f153734b25f400d4e7fb098` |
| 已核验索引 | `e815e3c6bea4310de1f564be881740c6ebf077dbe8c644d60681b43c77354fc7` |
| 原实验内容摘要 | `4814773e5b96237582512da461358ae837100362d4ba16d7aa7906e725bdf8bc` |
| 原实验代码（不是本轮代码） | `b488b0e282ef49cfbcceeb4bf84ca5aeb2d7e806` |
| 配置摘要 | `9810bbb0f0576924df7c1d8d9b8a06f2e7e787a206424d604aff6bc2925a3897` |
| 8E 成本原件 SHA-256 | `56177d9579b687b42f5c0cc31af0019d2e9e8d3ced936ff4bf475747dc461546` |
| 抽样种子／次数 | Python `Random(808061)`，2,000 次配对 UTC 日块 |
| 实际依赖环境 | Python 3.12.13，项目固定依赖；Node 24.18.0 仅用于 Bridge mock／类型／构建检查 |

最终收尾提交仅补早期输入路径拒绝、精确只读导入白名单、相应回归与交付文档。
上述整月研究仍明确绑定 `783a3212…`，不改标为最终收尾版本运行。
标签、统计、成本、特征计算文件在计算后没有变化；本轮不为发布文档重复运行
同一整月并累计两份证据。CLI 新的文件路径前置检查用合成测试验证。

未来留出：`PENDING`。最终研究策略、成本与选择标准冻结后，先另行确认
尚未读取的 UTC 起始日期，再使用连续 30 天固定区间。没有确定日期、读取新月、
下载新数据或承诺后台采集。五个周组仅是同一已见月份的稳定性描述。

## 3. 实现、只读边界与输出契约

调用链是 `显式冻结源 → past-only features → 独立未来标签 → 背景／块统计`
及 `原 8E 同数量记录 → 六档原 calculate_rr 反事实 → 成本边界`。
没有账户初始化、Broker、审批消费、执行锁、出站队列或新 schema。

原 SQLite 使用 `mode=ro`、`query_only=ON` 和拒写 authorizer，核对原应用 ID、
schema、实验／配置绑定；只读事务检查原表数量与 account payload 摘要不变。
这不是数据库文件字节哈希证明。路径先限制为指定 `quantified-runs/*/ledger.sqlite3`、
`historical-data/*`、`diagnostic-runs/*/cost-rows.jsonl`，再读输入／核对摘要；
拒绝链接、无关文件和未知数据库，不扫描父目录、`.env`、凭据或旧系统配置。
哈希只证明内容一致，不证明真实账户／数据供应认证。

结果必须写全新 `research-runs/` 子目录，已存在则拒绝；运行开始保存版本，
计算异常保存 `FAILED.json`，不覆盖失败工件。研究输入无 API Key 要求。
OS 离线沙箱禁止研究／Python 测试网络和原跟单系统访问；Bridge mock 仅开放本机回环。

| 文件／对象 | 内容与限制 |
| --- | --- |
| `candidate-features.jsonl` | 原 candidate_id、完整原件摘要、setup 摘要、方向、当时最后可见事件、原失效条件、事前证据；没有未来标签 |
| `future-labels.jsonl` | 分离的未来价格研究；各窗口 deadline、实际观察时间、双单位变化、MFE／MAE、首个观察失效、失效前 MFE、覆盖／截尾 |
| `direction-summary.json` | 所有窗口、多空／合并、原始／分层结果、日／小时／事前波动／周组、块区间和固定最早样本敏感性 |
| `cost-sensitivities.jsonl` | 原数量与依据、原件摘要、原拒绝原因、fee-only 与五档滑点、每档原 RR 版本、目标／止损成本腿、区间及无执行权限标志 |
| `cost-boundary-summary.json` | 多空分开、互斥主分类、共存的成本未识别标志、固定规则代表案例；不把 8E 的 98.32% 再算成新发现 |
| `summary.json`／`artifact-manifest.json` | 实際代码／协议／输入摘要、读取量、耗时、内存、零新审批／订单及各工件字节数和 SHA-256 |

摘要和代表案例上传 `validation/stage08fa/`，与本地原输出逐字节一致。
大文件未来标签、候选特征、逐行成本及原数据库不上传。原实验、原 8E 结果和
旧 `UNRESOLVED` 没有被覆盖；新结果不被原准入模块导入。

## 4. A 的标签、对照与不确定性规则

唯一主窗口 900 秒，辅助仅 60、300、3,600 秒。原 37,141 个候选全部保留：
LONG 18,655、SHORT 18,486；不按未来标签筛选原候选、改变方向或重生成计划。
信号时刻为原可见性时刻，价格为最后已可见 SOL 聚合成交；保持 250ms 可见延迟。
截止时点也用最后已可见事件，端点真实事件年龄必须小于 15 秒。
跨窗口出现至少 15 秒空缺则不足覆盖，截止时刻大于等于九月起点则截尾；
不向九月取数、不缩短窗口、不插值。仅已观察片段的极值仍可记录，但不进入完整标签比较。

方向符号 d 为 LONG +1 / SHORT −1。价格 P0、终点 P1：
`distance = d × (P1 − P0)`，`bps = 10000 × distance / P0`。
MFE、MAE 以相同方向定义，分别报告 bps 和 USDT/SOL，非初始 R 倍数。
15 秒索引保存区间全部聚合成交极值，不以抽样价近似极值；首个原止损价失效由
真实观察事件定位，保存 event_time 与 available_at、失效事件前的极值。
存在数据缺口时只能称“首个观察失效”，不是证明真实首次触价。

背景每个 UTC 15 分钟格点同时生成 LONG／SHORT，共 2,976 个时点、5,952 个
方向样本。与信号共用价格、窗口、缺失规则；背景没有虚构止损，失效标记
`NOT_APPLICABLE`。不因靠近信号而排除背景。

事前波动用最近已可见 300 秒 `(max-min)/last`，固定 bps 分界
0／10／25／50／100。联合层：方向 × UTC 日 × UTC 小时 × 波动档。
缺少同层背景的候选只记 UNMATCHED，不借邻层填满。主比较是同层差值按候选数加权，
同时完整披露未调整平均值。37,141 是依赖标签数，不是独立交易数。

不确定性整块重采样 31 个 UTC 起点日，2,000 次、固定种子，保留块内重叠及配对。
只在 31 天内近似，跨午夜标签和跨日序列相关仍可能使区间过窄；没有 iid t 检验、
显著性寻优或多窗口择优。辅助指标不经多重比较校正，不能作为额外确证。

## 5. A 的实际分母和结果

### 5.1 有效样本、截尾和缺失

| 窗口 | LONG 有效／原数 | SHORT 有效／原数 | 合并背景有效／原数 | 候选截尾／缺口 |
| --- | --- | --- | --- | --- |
| 1m 辅助 | 18,655 / 18,655 | 18,485 / 18,486 | 5,952 / 5,952 | 0 / 1 |
| 5m 辅助 | 18,653 / 18,655 | 18,480 / 18,486 | 5,952 / 5,952 | 3 / 5 |
| **15m 主** | **18,640 / 18,655** | **18,471 / 18,486** | **5,940 / 5,952** | **17 / 13** |
| 60m 辅助 | 18,594 / 18,655 | 18,435 / 18,486 | 5,904 / 5,952 | 50 / 62 |

主窗口有效 37,111，匹配背景 32,694（88.0979%）；4,417 未匹配。
所有窗口各方向排除标志与背景缺失数在上传 JSON 中，不能把失效／缺失样本当零收益。

### 5.2 主窗口：同时披露原始与分层结果（bps）

| 方向 | 原信号均值 | 原背景均值 | 未调整差 | 分层差 | 配对日块 95% 区间 |
| --- | ---: | ---: | ---: | ---: | --- |
| LONG | +1.5010 | +1.2132 | +0.2878 | +0.0509 | [−1.3999, +1.2811] |
| SHORT | −1.3663 | −1.2132 | −0.1531 | +2.2696 | [+0.9047, +3.5776] |
| 合并（唯一主指标） | +0.0739 | 0.0000 | +0.0739 | **+1.1535** | **[+0.4323, +1.8018]** |

合并背景均值为零是同一背景时点同时取正反方向的代数结果，不是市场没有趋势。
SHORT 的未调整方向收益仍为负；分层相对背景改善不能说成其绝对方向收益为正。
匹配样本、权重和背景构成不同，因此分层差与未调整差不能互换。

固定五周主差值为 +0.9193、+1.6859、+0.5154、+1.6514、+0.1903 bps；
对应匹配候选 4,702／4,395／5,882／13,400／4,315。第四周占比较高，
五周不等于五份独立验证。完整日、小时、波动档计数／均值保留在 JSON。

### 5.3 所有预先固定窗口；不能改称最佳窗口为主窗口

| 窗口 | 合并分层差 bps | 日块 95% 区间 | 每方向每 15m 最早候选的分层差 bps |
| --- | ---: | --- | ---: |
| 1m | −0.0093 | [−0.2532, +0.1677] | +0.1162 |
| 5m | +0.2691 | [−0.1474, +0.6682] | +0.0717 |
| **15m 主** | **+1.1535** | **[+0.4323, +1.8018]** | **+0.2082** |
| 60m | +3.0195 | [+2.1748, +3.7103] | +0.8346 |

主窗口最早候选口径有效 5,128、匹配 4,702；方向分开为 LONG −0.8323、
SHORT +1.2363 bps。它是固定的样本依赖敏感性，没有改成新的主估计，
也没有从“取最早”进一步搜索最优去重方法。60m 结果没有授权延长实际持仓。

### 5.4 路径先后和代表案例

| 15m 标签 | LONG | SHORT |
| --- | ---: | ---: |
| 完整窗口内观察到原结构失效 | 16,885 / 18,640 | 16,799 / 18,471 |
| 失效后出现比失效前更大有利变化 | 12,955 | 12,724 |
| 整窗 MFE 中位数 bps | 17.4311 | 16.8835 |
| 失效前 MFE 中位数 bps | 1.3191 | 1.3023 |
| MAE 中位数 bps | 17.4064 | 17.5913 |
| 终点方向变化 USDT/SOL p10／p50／p90 | −0.35 / +0.01 / +0.38 | −0.39 / 0 / +0.34 |

“失效前 MFE”没有观察失效时取整个已观察窗口；它也不是可实现利润。
合并 33,684 / 37,111 观察失效（90.7655%），25,679 个在失效后才出现更大 MFE。
这否定把整个窗口 MFE 直接归给原计划的做法，不证明方向信号在所有别的规则下均无用。

代表案例按协议最小 candidate_id 选取，不人工挑漂亮路径（完整 ID／时序在
`label-representatives.json`）：

- LONG `0000b7e35b6b…`：原价 104.50，信号后 4.656 秒观察失效；失效前
  MFE 0.02 USDT/SOL，整窗 0.16，终点 −0.44。后来的 0.16 不是原止损计划利润。
- SHORT `0007ebb91a7c…`：原价 76.60，信号后 226.356 秒观察失效；失效前
  MFE 0.06，整窗 0.18，终点 +0.13 USDT/SOL。正的终点标签同样不能掩盖先失效。
- 中位数规则另保留 LONG `7f8f3970762f…` 与 SHORT `013fba035a78…`，
  终点分别 +0.01、0 USDT/SOL，不用最高 MFE 选“成功交易”。

## 6. B 的数量、公式、假设与实际结果

逐候选复用 8E 原诊断数量 q、原计划和当前等级／市场有效最低净 RR k，
包括因原量化尚未完成而使用最小诊断数量的标签，绝不称作批准数量。
保留 q 对应的固定总资金费预算 F；不改变数量来提高 RR，也不重新生成更低资金费。

六个情景各自调用原第三阶段 `calculate_rr`，生成独立假设输入，不覆盖原 RR：

1. `FEES_ONLY`：原声明开／平仓费率，原参考价格，价差／滑点／资金费暂为零；
   明确乐观反事实，不作实际成交精度承诺。
2. 每腿滑点 **0、2、5、10、20 bps**：原声明完整价差 2 bps（每边一半）、
   买向上／卖向下 tick 取整、8D 保守整数合并 bps 投影、原声明费率及同数量 F。
   0 bps 不是无摩擦：仍保留价差、取整、手续费、资金费。

价格离散误差不能平滑插值；保持原保守出口投影覆盖止损及原目标中的最大不利
合并 bps。10 bps 的净目标收益、净初始风险和净 RR 逐候选与原 8E 结果一致，
不一致即停止。27 个原先未到量化规则回执的候选沿用 8E 明示的 0.01 tick
假设，单列 `8E_RETROSPECTIVE_DECLARED_TICK_0.01_NO_RECEIPT`，不称已验证交易所规则。

方向 d、参考入场 E、原结构止损 S、原目标 Ti、原分配 wi：

```text
gross_target = q × Σ[wi × d × (Ti − E)]
gross_stop_risk = q × |E − S|
G = gross_target − target_entry_fee − weighted_target_exit_fee
    − target_entry_friction − weighted_target_exit_friction − F
L = gross_stop_risk + stop_entry_fee + stop_exit_fee
    + stop_entry_friction + stop_exit_friction + F
necessary_static_condition = (G >= k × L), L > 0
```

fee-only 中 F／friction 为零，其余按假设独立生成；资金费是场景预算，不能把
目标腿与止损腿相加成同一次交易的双重费用。两条可能路径各有自己完整成本；
没有“目标扣费而风险不扣费”。金额 Decimal，价格索引无损整数，无法无损表示
给原模型的数值明确拒绝。两个守恒断言分别核对目标净现金流和止损净风险。

### 6.1 只计手续费的必要性检查

| 结果（互斥） | LONG | SHORT | 合并 |
| --- | ---: | ---: | ---: |
| 原目标收益不足／等于手续费 | 13,845 | 13,534 | **27,379** |
| 净目标为正但低于原静态净 RR 门槛 | 4,655 | 4,813 | **9,468** |
| 满足静态必要条件，仅反事实 | 155 | 139 | **294** |
| 总计 | 18,655 | 18,486 | 37,141 |

该 294 不是通过评分／情景／风险的机会数量，更不是 294 笔交易。

### 6.2 原资金费／价差不变，仅变每腿滑点

| 滑点 bps | LONG 非正／正但 RR 不足／静态必要满足 | SHORT 非正／正但 RR 不足／静态必要满足 | 合并静态必要满足 |
| ---: | --- | --- | ---: |
| 0 | 18,334 / 317 / 4 | 18,209 / 275 / 2 | 6 |
| 2 | 18,433 / 219 / 3 | 18,296 / 189 / 1 | 4 |
| 5 | 18,507 / 145 / 3 | 18,372 / 113 / 1 | 4 |
| 10 原档位 | 18,575 / 79 / 1 | 18,426 / 59 / 1 | 2 |
| 20 | 18,625 / 30 / 0 | 18,471 / 14 / 1 | 1 |

主归因互斥顺序：手续费耗尽 → fee-only 正但 RR 不足 → fee-only 可行却在
原资金费／价差、零滑点仍不达标 → 仅较低滑点档位达标 → 当前静态必要条件。
最后三类分别为 LONG 151／3／1，SHORT 137／1／1。总数仍是 37,141；
“真实成本证据未知”另作共存标志，对全体成立，不重复累计成额外机会。

### 6.3 固定格点成本区间与代表案例

37,135 个在全部固定滑点档位均不满足静态必要条件；5 个只有区间边界，
1 个到 20 bps 仍满足（不是最大可承受滑点已找到）。无连续光滑阈值外推。
只在同 q、同原资金费预算下报告相邻实测档位通过／失败；不据此改成本或审批。

按最小 ID／主分类固定选择的案例：

| 方向／candidate_id 前缀 | 数量 SOL | 结论与不能省略的条件 |
| --- | ---: | --- |
| LONG `0000a9e546e5…` | 6.768 | fee-only 净目标 −0.29511864 USDT，手续费已吃掉原目标 |
| SHORT `00126345a407…` | 5.142 | fee-only 净目标 −0.113393955 USDT，同类费用必要性失败 |
| LONG `00c0bc571f22…` | 5.000 | fee-only 为正且达标；保留资金费／价差后即使零滑点仍失败 |
| SHORT `0c543e15960a…` | 5.191 | 同上；不得把“删除资金费的反事实”改作正式成本 |
| LONG `3ec94eb9f17b…` | 0.055 | 5 bps 通过、10 bps 失败，只能报区间；另有原 BTC 风控拒绝 |
| SHORT `2b2a3b728b5e…` | 4.674 | 0 bps 通过、2 bps 失败，只能报区间；原当前成本下 RR 不足 |
| LONG `fbcfdb9dad7e…` | 0.054 | 当前 10 bps 静态 RR 2.1916，20 bps 失败；原 `BTC_CRASH_LONG_BLOCK` 仍在 |
| SHORT `3ca7e4ecdd0c…` | 5.329 | 当前静态 RR 4.0440，全部档位静态满足；原 `QUANTIFICATION_UNRESOLVED`／TP2 情景限制仍在，8E 提前保护描述不替换审批 |

完整 ID、假设、六档每腿金额、输入摘要及原拒绝码见上传成本 JSON；没有人为
挑选盈利图例，也没有把静态目标估值转换成 Runner 统计期望。

## 7. 哪些成本可识别，哪些仍不可识别

| 项目 | 本轮实际证据 | 不可作出的结论／后续另行授权的数据 |
| --- | --- | --- |
| 手续费 | 原计划声明费率（示例每腿 0.0005）；固定费率算术可核对 | 未核验账户等级、maker/taker 或优惠；需相应历史账户规则／成交费记录 |
| 价差 | 原模型完整价差 2 bps | 无对应时点买卖盘口；相邻成交差不是可成交价差；需历史双边报价和时间／可见性对齐 |
| 滑点／冲击 | 原 10 bps 与固定 0/2/5/10/20 反事实 | 聚合成交量不是盘口深度；无自有订单、到达时刻、排队及成交因果链；不能标定本系统真实冲击 |
| 资金费 | 8E 原 q 物化的事前固定预算 | 不是研究路径已支付费用；不利用随后结算回填更低预算；真实持仓与结算资料本轮未取 |
| 价格／数量规则 | 原声明 tick／step 和 q；缺规则回执的 27 个候选明示沿用假设 | 非历史真实交易所能力确认；需有时效的历史规则证据，不能由 source 名或哈希认证 |

因此只能说明“在所声明模型下需要怎样的成本条件”，不能判定真实可执行性。
成本过保守与否尚无独立标定证据，不以该怀疑放宽预算。

## 8. 实际运行范围、性能与不可重复累计的证据

本轮只进行一次完成的八月标签研究／六档成本 pass；未运行两套整月账户。

| 项目 | 实际值 |
| --- | ---: |
| 冻结候选特征行 | 37,141 |
| 信号／背景全窗口标签行 | 172,372 |
| 成本输出行／每行六档 | 37,141 / 222,846 个情景 |
| SOL 单次流式解析 | 7,808,252 条，499,996,301 字节（含原预热） |
| 输入原件／索引文件及成本原件哈希校验读取 | 6,136,057,752 字节 |
| 15 秒索引桶 | 178,581（包括预热和尾边界） |
| 整次运行耗时 | 254.5164 秒 |
| 进程峰值 RSS | 428,097,536 字节，408.27 MiB |
| 月外可见事件／新审批／新订单 | 0 / 0 / 0 |

读取量是工具统计的文件 hash／SOL 解析字节，不声称覆盖 SQLite 页缓存或所有
系统 I/O；非 SOL 文件仅做原清单 hash 校验，未为每个候选重扫全月。
极值索引上限 180,000 桶、特征 40,000，运行上限 60 分钟／2 GiB；超界明确失败。
本地大工件保留，不上传数据库、交易日志、账户标识或密钥。

特征链 `85ee5169bb1aa1cc9737a06c297b8d9fe4344e51c194c304684ebdb18af22245`；
标签链 `b43e44e377102eba9b223946a1e0b1773761b88ab56272d51398b2c2591c2d8b`；
成本链 `5efac5b16d4af101ead3a2fa8a540b34a3e59d37dcb0cc562d03985aa6d34a07`。
逐文件 SHA-256 见 `validation/stage08fa/artifact-manifest.json`。

## 9. 测试设计、失败保留与实际回归

新增 69 项参数化测试：未来修改不改变原候选摘要；生成端／标签端分离；
恰好截止、可见延迟、月末截尾、缺口、重复 ID；多空镜像和逐 tick 独立标签 oracle；
先失效后盈利、MFE 非利润；信号／背景同规则、同层匹配不借别日别方向、
重叠日块与固定最早候选；手续费／滑点／资金费、同 q 两腿守恒、离散 tick、
资金费未填零、未知实际成本；真实只读 SQLite 写入拒绝、输入前置拒绝、
导入不构建 Broker／无网络账户副作用。源库合成设置只用于测试，不是研究入口。

没有删旧回归、跳过或放宽业务断言。静态导入白名单只精确允许新只读模块：
8E 的旧“任何外部模块不得引用”断言增加唯一 `app/signal_research/cli.py`
例外，并 AST 精确限定其只从 `app.scenario_diagnostics.source` 导入
`frozen_source, bodies`；仍不允许接入旧运行路径或执行模块。

失败运行按实际顺序保留，不拿重复运行累计测试数量：

| 运行 | 实际结果 | 原因／处理 |
| --- | --- | --- |
| 初始合成原型（提交 `fbf2e6de…` 保留） | 6 failed / 38 passed | 背景 UTC 网格锚点、Decimal 序列化、合成源未显式持久化 prepare、误经 8E CLI 引入 Broker；修正边界／测试安排，不改断言 |
| 同组修复后 | 44 passed，3.48s | 先完成合成时序验证 |
| 扩展／相关回归首次 | 6 failed / 387 passed | 新用例从二进制浮点构造 Decimal，触发应有无损精度拒绝；改测试输入为十进制字符串，断言不变 |
| 扩展／相关回归重跑 | 393 passed，7.20s | 对应 67 项新增测试与相关旧模块；不与全量相加 |
| 实际八月研究 | COMPLETE，254.5164s | 无失败整月运行；保留本轮独立目录和计算提交 |
| 首次全量 Python | 1 failed / 2,308 passed，286.88s | 8E 静态禁止外部引用未包含新只读消费者；改为上述精确 source 白名单 |
| 后发现的输入路径前置专项 | 2 failed / 67 deselected，0.27s | 测试用不存在的 `.env`／无关文件名及 hash spy 证明读取顺序不足；未实际读取凭据；补读前路径拒绝 |
| 最新 69 项＋8E 只读集成 | 70 passed，3.96s | 2 项原失败断言保留；没有改研究计算 |

最终实际检查（不与上述重跑重复累计，不沿用 8E 旧数字）：

| 检查 | 实际命令 | 实际结果 |
| --- | --- | --- |
| Python 全量 | `python -m pytest -q` | **2,311 passed，2 warnings，236.45s** |
| 静态隔离 | `python scripts/verify_isolation.py` | `ISOLATION_SOURCE_PASS: 130 Python files` |
| Bridge mock（独立计数） | `node --import tsx --test tests/*.test.ts` | **27 passed，0 failed／skipped，572.100958ms** |
| Bridge 类型 | `node node_modules/typescript/bin/tsc --noEmit` | 退出码 0 |
| Bridge 构建 | `node scripts/build.mjs` | 退出码 0；6 个隔离 vendor、82 个输入、standalone import 通过 |

Python 的两条警告来自 Starlette/httpx 与 anyio 别名弃用，不是跳过测试；
本轮未借机升级依赖。Bridge 输出里的订单是 mock 合成对象，不是真实下单。
2,311 已包含 69 个本轮新增参数化用例及旧回归，不能再加 393／70；
Bridge 的重复检查也只记 27 项。以上都是开发方本轮实际证据，不冒称独立审查方复验。

## 10. 可复制命令与版本说明

先使用项目 Python 3.12 独立环境及 `requirements.lock.txt`，不用 API Key、旧服务或
真实 `.env`。首次检出若没有本地已核验历史数据／原实验库，只能运行合成测试和
检查已上传摘要，不能用空库／重新生成候选伪装原研究已重放。

```sh
python -m pytest -q tests/test_stage08fa_research.py
python scripts/verify_isolation.py
python -m pytest -q
```

复现本次计算请检出已推送的计算提交 `783a3212b26e142a11cd308713738e313f43737c`，
使用已核验输入，并给全新输出目录（已有 `august-8fa-v1` 不能覆盖）：

```sh
python -u -m app.signal_research.cli \
  --source "$PWD/quantified-runs/august-8d-baseline-v2/ledger.sqlite3" \
  --dataset "$PWD/historical-data/august-2026-v1" \
  --cost-rows "$PWD/diagnostic-runs/8e-full-v1/cost-rows.jsonl" \
  --output "$PWD/research-runs/august-8fa-v1-reproduction" \
  --code-commit 783a3212b26e142a11cd308713738e313f43737c
```

最终收尾版本同样支持命令，但必须记录自己的实际代码提交，不能用旧 SHA 冒充。
`--code-commit` 是调用者声明，另有实际模块文件摘要交叉核对；哈希不是签名授权。
摘要示例：`status=COMPLETE, candidate_count=37141, sensitivity_rows=222846,
orders_created=0, approvals_created=0, holdout=PENDING, live_allowed=false`。

Bridge 检查在 `bridge/`，项目 Node 24 固定依赖：

```sh
node --import tsx --test tests/*.test.ts
node node_modules/typescript/bin/tsc --noEmit
node scripts/build.mjs
```

研究／Python 检查实际加 OS 离线沙箱；Bridge mock 仅允许本机回环，无交易所访问。

## 11. 本阶段新增／修改文件与原行为

新增：

- `docs/STAGE_08FA_RESEARCH_PROTOCOL.md`：已先提交、后计算的固定协议。
- `app/signal_research/__init__.py`、`features.py`、`labels.py`、`statistics.py`、
  `costs.py`、`io.py`、`cli.py`：七个独立只读研究文件。
- `tests/test_stage08fa_research.py`：合成／只读边界与数值回归。
- `STAGE_08FA_REPORT.md`：本报告。
- `validation/stage08fa/artifact-manifest.json`、`summary.json`、`direction-summary.json`、
  `cost-boundary-summary.json`、`label-representatives.json`：本次实际输出的脱敏摘要／固定案例。
- `validation/stage08fa/development-checks.json`：测试与失败记录，独立于统计结果。

修改：

- `README.md`：本阶段入口、结果及旧阶段说明边界。
- `.gitignore`：忽略独立研究原始工件目录。
- `scripts/verify_isolation.py`：仅新 labels／CLI 的 `heapq`／`resource` 精确白名单。
- `tests/test_admission.py`、`tests/test_dynamic_rr.py`、`tests/test_stage08e_diagnostics.py`：
  精确的新只读消费者白名单，保留原执行隔离和业务断言。

原 `app/` 下除新增研究目录外所有实现文件、原 `main.py`、配置、准入、RR、评分、
退出状态机、8A—8E、Bridge、实盘封禁均不修改。没有迁移数据库或改写历史实验。

## 12. 未运行、限制及下一步建议（不实施新策略）

已实现／实际运行：只读标签、背景分层、块不确定性、固定成本边界、合成验证和
一次完整固定八月研究。没有运行新账户、交易回测或策略利润计算；`simulated_profit`
与 `equity_drawdown` 为 null。零新订单是研究边界，不是“策略安全／没有机会”的证明。

未完成／未验证：留出月份、真实费率／盘口／自有冲击标定、真实资金费支出、
新策略有效性、Runner 统计期望、实时／测试网／真实账户与实盘。本轮没有再次
穷举 8E 数量或重跑其证书，也不把其旧测试数加到本轮证据。无部署或 Telegram。

本轮**不提出立即扩大结构窗口或拉远目标的新策略**。理由：相对背景的弱方向线索
主要在 SHORT，最早候选敏感性明显减弱；原失效前有利尺度远小于整窗 MFE；
即便只计声明手续费，大部分原目标已经不可行。这些证据不足以在已见八月上
直接选一个更远目标／更长持有期并称作修复。

若后续继续，首先需单独确认研究选择标准及成本验证所需资料权限；不能把假设
低滑点当已标定。如果将来决定另立新结构尺度策略版本，必须单独冻结变量、
结构形成与确认时间、新鲜度契约、保持不变的风控与成本、以及未见区间；不能
把旧结构时间戳改成当前。当前没有实施新信号或新审批，也不自动进入 8F-B。

保持分支历史，不修改／合并 main、不 force push；交付后暂停等待验收。
