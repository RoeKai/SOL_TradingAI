# Stage 8E — 情景描述、静态证明独立审计与只读成本诊断

独立分支：`phase-08e-scenario-diagnostics`。
唯一开发基线：`84c525cd613a039aed22821b9290c5992056eef9`。
本轮业务源码提交：`ea39c11acaaf35ef9b0c01e332ad65dfde544213`；报告与脱敏证据为其后续提交。
实际运行的 app + 五份配置模板内容摘要：
`df410d39e98316b0016e4fa67fb724feb8a37730133e30165242c1b2fa24e705`。
测试和诊断在提交前执行，提交树与已执行文件逐字核对。收尾时仅校齐新增TP2合成测试的
计划比例元数据，使它与该测试的可配置政策一致，原断言不变；业务源码和诊断摘要未改变。

## 1. 结论与验收范围

本轮只新增**独立、显式调用的描述与审计工具**，不替换8D默认模型。

* 唯一未解决候选的 `0.054–5.329 SOL`、步长 `0.001` 共 **5,276** 个数量全部核验。
  每个数量分别确认原退出状态机发出的实际动作：均在TP1后提前保护退出，均未进入S3所要求的Runner参考路径。
* **37,107／37,107** 份静态全域拒绝证书通过独立校验，0不一致、0未支持；
  固定选择规则选出11个不同历史候选，共 **704** 个格点调用原 `calculate_rr` 交叉核验，0差异。
  证书全量核验不等于所有历史数量逐点穷举。
* 同一八月 **37,141** 候选全部完成只读成本分布，未改变信号、日期、币种或参数。
  **36,518个（98.32%）** 在不计资金费预算时，交易成本已经覆盖全部加权目标收益。
* 本轮开发方全量回归：**2,242 Python通过、2条既有依赖弃用警告；27 Bridge mock通过**。
  新增55个Python参数化用例已包含在2,242中，不能再相加。

8D审查方39项数值组件检查（含1项已披露限制的预期异常）与开发方
2,187 Python＋27 Bridge历史记录原样保留，不与本轮混算。审查方未独立重跑完整quantify、审批、CLI或整月。
本轮没有连续账户整月复跑，没有证明策略有效性或实盘可用。

## 2. 实际复现及新情景契约

旧8D函数仍保留原行为。新增多空测试先调用旧 `evaluate_scenarios`，
原样复现 `SCENARIO_EXPECTED_ACTION_MISSING:TP2`，再检验新描述器。
没有修改旧函数使这个预期异常消失。

新版本：`protective-path-description/v1`。
输入仍是已按独立数量物化的 `QuantifiedExitPlan`、只用于条件实验的快照和q；
复用 `SpreadPath` 的价格、手续费及确认事件驱动，**盈亏和剩余成本仍由原Exit reducer记账**。
实验快照使用REJECT、无执行权限；不是伪造审批，不构造Broker订单或真实持仓。

核心顺序：

1. 确认首笔合成入场，按原规则冻结R；确认保护与入场腿终态。
2. TP价格到达只产生意图；仍显式接受并分片确认TP成交。
3. 每档确认后先检查原状态机。如果已进入 `STOP_PENDING`，确认原控制动作，
   以**当前可见模型报价**确认它已经提出的 `CLOSE_ALL`，不发送不会产生的下一档TP。
4. 只有正常到达参考结构位置后才记 `RUNNER_REFERENCE`；回撤、止损和费用继续沿用原规则。
5. 对全部退出数量、独立成交金额差、手续费和原reducer的毛／净盈亏作守恒断言。

输出 `Description` 包含：版本、scope、scenario_id、plan_digest、quantity、capability、premise、
reason_codes、分腿现金流、完整合成确认事件trace及摘要、最终状态摘要、实际到达阶段、
剩余数量、毛盈亏、手续费、独立资金费预算、条件净现金、冻结R、最终止损、
S0初始净风险、条件现金RR、scenario_net_rr、expected_return及execution_authority。

| 情况 | 新结果 | 含义 |
|---|---|---|
| 已确认规定路径 | `FULFILLED` | 仅该条件路径的现金流，不是期望收益 |
| 原保护提前终止路径 | `EARLY_PROTECTIVE_TERMINATION` | 退出已结清；后续S3前提没有发生 |
| 缺少合格结构参考 | `UNAVAILABLE` | 不生成远端目标，不假定到达 |
| 已声明的模型能力缺口 | `UNSUPPORTED`／`MODEL_UNSUPPORTED` | 不当作普通通过或数量无解 |
| 诊断数量域未遍历完 | `complete=false`＋未完成区间 | 独立于正式求解预算耗尽 |
| 未预期异常／缺少应存在的动作 | 抛出错误，CLI非零 | 不重分类为UNRESOLVED掩盖程序错误 |

提前终止时 `scenario_net_rr=null`，但已发生的条件现金可以计算
`conditional_cash_rr` 用于诊断。`expected_return=null` 永远不由这些路径填入。
未知对账或旧float接口不能无损表示时，只报告明确能力缺口；其他异常不吞掉。
不存在“所有异常统一UNRESOLVED”的新catch。

## 3. 唯一候选：逐数量补验

引用并核验旧 `validation/stage08d/unresolved-diagnostic.json`：
原归因candidate ID `85cee1dc0d7b0ed540e73908114744712989ddbd5f4396c4cbd1b718010b60ef`；
连续8D实验ID `3ca7e4ecdd0c88b593f2d4c7e2003c4e3ad7420aa24db8a91ac53d2fb3a8c46d`。
不同实验的ID不强行等同；核对时间1787375640、SHORT、93.70、93.59、93.72、q5.329及R0.13。
输入及参考摘要见 [audit-summary](validation/stage08e/audit-summary.json)。

q=5.329的确认现金流（全部是条件实验，不是历史真实成交）：

| 动作 | 确认数量SOL | 模型成交价 | 手续费USDT |
|---|---:|---:|---:|
| ENTRY |5.329|93.59|0.249370555|
| TP1分片1 |0.799|93.56|0.037377220|
| TP1分片2 |0.799|93.56|0.037377220|
| CLOSE_ALL分片1 |1.865|93.56|0.087244700|
| CLOSE_ALL分片2 |1.866|93.56|0.087291480|

TP1合计1.598后触发 `PROPOSED_STOP_ALREADY_CROSSED`。
剩余3.731由原状态机全退，无TP2；总退出5.329，剩余0。
毛盈亏0.15987，手续费0.498661175，资金费**预算**1.00004014，
条件净现金 **−1.338831315 USDT**。
S0风险2.725064085，提前终止现金RR约−0.49130269；**不是成功S3的RR**。
原静态净RR4.04396289665保持不变，不能代替此路径现金流。

| 数量 | 毛盈亏 | 手续费 | 资金费预算 | 提前退出净现金 |
|---:|---:|---:|---:|---:|
|5.329|0.15987|0.498661175|1.00004014|−1.338831315|
|2.691|0.08073|0.251810325|0.50499306|−0.676073385|
|0.054|0.00162|0.005053050|0.01013364|−0.013566690|

以上是确定性首／中／末代表；实际逐点计算了全部5,276个数量，各跑S0—S3描述。
全部剩余数量数值为0；所有S3均前提未成立、`scenario_net_rr=null`，未完成点为0。
完整逐数量JSON留在本地独立诊断目录；提交的
[artifact-manifest](validation/stage08e/artifact-manifest.json)记录文件字节数、SHA-256、范围、代表点和完整行摘要链。
[unique-first-quantity](validation/stage08e/unique-first-quantity.json)提供首数量的完整确认事件。

此结果证明**这一既定条件路径**在该有限域内都提前结束；不是对其他真实市场路径、
所有可能S3、全部准入条件的无解证明。没有重跑每个数量的评分／正式审批。
旧8D正式32次预算、原UNRESOLVED和整月报告不变，新模块不会自动替代它。

## 4. 独立静态全域证明审计

审计器不导入或调用 `quantify`／`admit_trade`，不取生产器最终等级作为全域边界。
先验证原始结构与派生输入的身份、内容、成本数量、版本和配置绑定，
独立从价格、原目标权重、费率和保守bps映射推导每币系数。

设方向d为LONG +1、SHORT −1；原参考P、止损S、目标T_i、权重w_i，Σw_i=1。
入场/退出不利预算e、x是小数比率，手续费率a、b也是小数比率：

```
E = P + d*P*e
X_i = T_i - d*T_i*x
C(T_i) = P*e + T_i*x + E*a + X_i*b
g = Σ w_i*[d*(T_i-P) - C(T_i)] - funding_per_unit
l = d*(P-S) + C(S) + funding_per_unit
f0 = 明确的整笔固定成本
G(q) = g*q - f0
L(q) = l*q + f0 > 0
G(q)/L(q) >= k  <=>  (g-k*l)*q >= (1+k)*f0
```

金额USDT，q为SOL；g、l为USDT/SOL，f0为USDT。
比率0.01为1%；bps先除10000，原50/50目标分配保留，不换为30/40/30。
当前8D固定项0是已声明模型语义，不是缺失资金费填0；线性资金费来自冻结函数。

对每个等级分别重建请求、单笔、计划、日剩余、权益风险、等级及市场风险上限，
以及保证金、杠杆、名义额、数量和交易规则限制。取所有等级数量域的外包区间。
区间包含可能因评分或最小风险被淘汰的点，因此是保守上界，不漏掉更低等级的可用数量。
k为原全局／市场下限的较高值，逐一验证所有等级有效门槛不低于k。

用 `(g-k*l)*q` 在区间两端的最大值与 `(1+k)*f0` 作严格比较；
同时保留导数 `f0*(g+l)/L(q)^2` 的成立条件。`g+l<=0` 时G为负，不能正RR放行。
阈值判定不加容差；冗余50位Decimal序列化比值核对允许1e−45的舍入差，仅用于记录一致性。

限定条件：精确单点入场、完整真实结构目标权重、非负明确费用、正净初始风险、线性成本。
旧float输入的整域无损表示使用保守有效位条件；不能证明则UNSUPPORTED，不能只检查端点后假定内部都可表示。
范围入场、版本未知、成本缺失、结构替换或绑定不一致不得静默按最新语义验证。
**此定理不用于分批取整、提前止损、Runner或其他路径相关S3。**

实际证据：

* 前1000条开发探查及其样本不是额外独立样本；最终以完整清单为准，不重复累计。
* 完整37,141条历史记录中37,107条带静态证书，全部完成检查；0差异、0UNSUPPORTED。
* 先固定抽取规则，再按LONG／SHORT各选：最近RR门槛、最窄止损、最高／最低成本、
  最大／最小数量域及q_max，重复选择同候选只计一次，哈希ID打破并列。
* 共11个不同候选，各64个等距整数格点，包含上下边界：**704次原calculate_rr复算，0差异**。
* 没有对37,107个候选的所有格点穷举。数值证据范围与代数证书验证范围分开。

逐证书记录完整输入摘要、版本、g/l/f0、q_min/q_max/step、全局/市场/等级门槛、
各等级上界、严格不等式两边、单调条件与差异。
完整本地证书清单摘要链：`e4eed3b077fbc4668f9ab0be21af89a24a43119d6f6c81884c2033ff9d62644e`。
提交 [11份样本证书](validation/stage08e/sample-certificates.json)，完整文件SHA在manifest。
哈希证明内容绑定，不证明市场真实性或账户权限。

## 5. 信号／成本诊断与互斥归因

同一八月清单：LONG18,655、SHORT18,486。全部37,141条都有方向和只读成本描述。
有已计算数量域者取硬上界q作为**诊断数量，不是下单数量**；尚未定量的条目使用原规则最小合法数量，
明确标记未审批。每条成本、净收益和风险都使用同一q；F(q)仍按原函数物化。
27条在旧路径因报价过期而提前返回的条目只回看当时数据、使用冻结模拟规则描述成本，
不更新报价时间、不宣称恢复新鲜、不补审批，主分类E。另1个E为原未解决数量搜索。

主分类优先级E→A→B→C→D，同一机会只计一次；原共存拒绝原因保留为reason_codes。

| 互斥主分类 | LONG | SHORT | 合计 |
|---|---:|---:|---:|
|A：不计资金费，成本已覆盖目标收益|18,318|18,200|36,518|
|B：加资金费预算后才非正净收益|241|215|456|
|C：净收益正，但低于原有效RR门槛|79|59|138|
|D：静态合格，其他准入原因阻断|1|0|1|
|E：未完整定量／情景前提未满足等未解范围|16|12|28|
|总计|18,655|18,486|37,141|

D的LONG为BTC同步下跌限制，保留原原因，不修改大盘门槛。
SHORT最大静态RR属于上述提前终止候选，仍在E，不把高静态RR改记为S3通过。

代表分布（每SOL的USDT距离／预算；不是实际扣费）：

| 项目 | LONG p50 | SHORT p50 | LONG p90 | SHORT p90 |
|---|---:|---:|---:|---:|
|结构止损距离|.020|.020|.070|.070|
|原分配加权目标收益距离|.050|.055|.165|.160|
|双边手续费|.093862449|.093739976|.105047491|.104874982|
|双边价差一阶项|.0187725|.018748|.0210095|.020975|
|双边滑点一阶项|.187725|.187480|.210095|.209750|
|复合／tick／整bps保守映射残差|.019420|.0193725|.029470|.029479|
|资金费预算|.18820|.18784|.21060|.21016|
|静态净RR|−.8452794|−.8420810|−.6141529|−.6235292|

加法分解：模型入出总不利bps先拆出一阶价差和一阶滑点，余项保留复合、tick及保守整bps影响，
不冒称已精确分离历史盘口点差与真实冲击。总手续费按RR模型有效价格计算；各项相加回到同q净目标收益。
所有min/p10/p25/p50/p75/p90/max、同q金额与风险见
[cost-distributions](validation/stage08e/cost-distributions.json)。分位数为排序后floor((N−1)p)，不插值。
每方向每主分类的代表固定取最小candidate ID，文件中给出完整ID、时间、数量、成本、净RR和共存原因。
不按盈利或好看的RR挑代表。

反事实“不计资金费”仅用于A/B归因，不生成新审批。
手续费预算也不是实际发生费用。作为额外交叉描述：手续费一项就不小于加权目标距离的
候选有LONG13,845、SHORT13,534；滑点一阶项单独覆盖目标的有LONG17,332、SHORT17,251。
这些是共存原因统计，不能与主分类相加成更多机会。

### 下一步研究判断（本轮不实施）

**主要矛盾是当前信号结构尺度与冻结执行成本不匹配，不只是资金费预算。**
约98.32%的候选在资金费加入前已无正净目标空间。双边滑点假设是最大的非资金费项；
目标距离中位数.05/.055，甚至低于手续费中位数约.094。

模型假设：每腿10bps滑点、2bps总点差、tick/整bps保守映射、资金费率.001×2次预算、
可执行报价及条件成交顺序均不是历史L2重建或真实成交证据。
信号本身：当前15秒采样、300秒窗口、精确重获与最近结构目标频繁给出很窄的止损／收益距离。
这种尺度事实与是否真的有方向预测能力是两个问题。

有足够理由**另立受控的新信号研究版本**，但没有理由宣称现策略有优势，或降低本版成本／RR门槛强求成交。
建议后续分别预注册“结构尺度研究”和“历史可执行成本标定”，使用独立样本外区间，
与冻结版做归因对照；窗口、目标规则等任何改变必须另行授权。
本轮未修改采样、窗口、精确重获、最近目标、止损、退出比例、评分、RR或风险门槛。

## 6. 隔离、文件及行为影响

新增源码7个：

* `app/scenario_diagnostics/__init__.py`：独立模块声明，无自动接入。
* `scenarios.py`：版本化情景与原reducer驱动。
* `proofs.py`：独立系数／等级域／静态证书和原RR格点交叉检查。
* `source.py`：明确指定的8D只读SQLite入口，校验应用ID1397705787/schema4、配置与实例绑定。
* `costs.py`：同数量成本分解、主分类、分位数及固定代表规则。
* `quantities.py`：逐数量诊断、旧材料几何核验，不调用正式求解器。
* `cli.py`：显式离线命令，新输出目录，不覆盖旧实验。

新增：`tests/test_stage08e_diagnostics.py`、本报告；
`validation/stage08e/` 下5份JSON：audit-summary、artifact-manifest、cost-distributions、
sample-certificates、unique-first-quantity。

修改8个：`.gitignore`、`README.md`、`scripts/verify_isolation.py`，以及
`tests/test_admission.py`、`test_config_cli_isolation.py`、`test_dynamic_rr.py`、
`test_exit_policy.py`、`test_scorecard.py`。
既有测试仅精确增加新诊断文件的静态白名单，业务断言未删／未放宽；SQLite仅增加source.py一个明确只读入口。

原 `app/exits`、`app/execution_costs`、`app/historical_replay`、旧Paper、main、Bridge和配置模板字节不变。
原实验与8D报告未修改。源库核验后仍为：订单0、成交0、持仓0、预留0、outbox0、审批消费0、现金500。
使用SQLite `mode=ro`、query_only及写操作authorizer拦截，不初始化／恢复实例，不处理出站队列。
这不是声称做过源DB前后全文件哈希比较；实际检查方式与范围写入manifest。

不读取.env，不连接网络／账户，不发送Telegram，不调用私有Bridge，不创建订单，不迁移数据库，不部署。
离线测试使用OS网络/凭据阻断；Bridge测试只允许本机mock回环。
完整诊断输出约128MB留在 `diagnostic-runs/8e-full-v1/`，被.gitignore排除；
只提交源码、测试、文档及脱敏审计摘要，不上传账户状态、数据库、日志、密钥或数据归档。

## 7. 实际命令、结果与限制

项目Python3.12.13环境；以下python指项目虚拟环境，不需要任何真实Key。

```bash
python -m pytest -q tests/test_stage08e_diagnostics.py
python -m pytest -q
python scripts/verify_isolation.py

python -m app.scenario_diagnostics.cli \
  --source "$PWD/quantified-runs/august-8d-baseline-v2/ledger.sqlite3" \
  --output "$PWD/diagnostic-runs/8e-full-v1" \
  --sample-points 64 \
  --unresolved-reference "$PWD/validation/stage08d/unresolved-diagnostic.json"

# bridge目录，Node24.18.0；仅本地mock沙盒
node --import tsx --test tests/*.test.ts
node node_modules/typescript/bin/tsc --noEmit
node scripts/build.mjs
```

实际Python命令外层为无网络／凭据读取阻断的sandbox-exec；Bridge mock使用只允许回环的隔离配置。
直接tsx CLI第一次因沙盒禁止IPC管道而未启动测试，改用Node原生测试入口，未放宽外部网络权限。

| 检查 | 实际结果 |
|---|---|
|早期8E专项|38通过（新增扩展前；不重复累计）|
|发布前完整8E专项|55通过，13.23秒（已包含在全量内，不再相加）|
|专项＋旧纯模块边界|556通过（中间检查，不与全量相加）|
|首次Python全量|2,242通过，2 warnings，238.65秒；不与最终重跑累加|
|最终发布内容Python全量|2,242通过，2 warnings，262.72秒；其中新增55项|
|Bridge mock|27通过，0失败，0跳过|
|Bridge类型检查|通过|
|Bridge构建／vendor／独立导入|6 vendor快照、82构建输入，独立导入通过|
|源码隔离|123 Python文件通过|
|完整离线CLI|退出0；37,141记录、37,107证书全部完成，0差异|
|历史样本原RR格点|11候选×64点=704，0差异；非全域穷举|
|唯一候选条件描述|5,276数量完整覆盖；无未完成区间；均提前保护|

测试涵盖：多空TP1及TP2后提前保护、未进入Runner分类、原版本不替换、
重复确认不重复减仓／收费、现金守恒、正常Runner回撤与旧结果一致、缺结构不可用、
单数量失败不能证明全域无解、原RR全选定格点复算、固定成本代数、等级范围、
证书/目标/费用/比例/版本/数量域篡改、A—E互斥分类、只读SQLite、CLI完整路径、
原运行路径不接入和程序错误不伪装成搜索耗尽。
8A—8D及退出R1/R2/R3、模型越界保护的原回归全部保留。

示例机器摘要：

```json
{"statistics":{"candidate_records":37141,"certificates":37107,"valid":37107,"inconsistent":0,"unsupported":0},
 "inventory_complete":true,"differences":[],"orders_created":0,"approvals_created":0,"live_allowed":false}
```

未运行：第二遍整月连续账户、实时行情、真实历史盘口、真实账户／测试网、真实强平模型、实盘。
没有新增胜率／期望收益预测器。当前结果不足以宣称策略有效、实际成本已标定或历史普通交易被验证。
新版仅描述所支持条件路径；旧8D量化入口仍保留其原限制。后续若要用它改变准入，需要单独版本化授权和验收。

## 8. 交付与暂停

仅推送 `phase-08e-scenario-diagnostics`，保留84c冻结历史，不合并／修改main，不force push。
交付提交包含本报告与可核对的脱敏证据，最终完整commit hash由本轮交付回复提供。
完成后暂停，等待8E验收，不自行开展策略优化或下一阶段。
